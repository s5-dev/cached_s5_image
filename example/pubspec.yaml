name: cached_s5_image_example
description: "A test app for cached s5 image."
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=3.4.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  cached_s5_image:
    path: ../
  s5: ^0.2.0
  hive: ^2.2.3
  logger: ^2.4.0
  cached_s5_manager: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
